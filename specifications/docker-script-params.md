1. I'd like to add parameters to the script bin/server-docker-ingest-start for DOCKER_NETWORK and MONGO_URI, with wrapper scripts to call that script from the two docker-compose scenarios (in docker/docker-compose/[mongo-replica-set-single, and mongo-replica-set-multi].  The wrapper scripts will pass the correct docker network name and mongo connect string for the docker compose scenario so that an ingestion server is started using the supplied parameter values. The parameter values should be mapped as follows: 1) mongo-replica-set-single network = "mongo-replica-set-single_default", mongo URI = "mongodb://admin:admin@localhost:27017/?replicaSet=rs0&authSource=admin"; 2) mongo-replica-set-multi network = "mongo-replica-set-multi_mongo-cluster", mongo URI = "mongodb://admin:admin@mongo1:27017,mongo2:27017,mongo3:27017/?replicaSet=rs0&authSource=admin".

2. I'd like to add parameters for DOCKER_NETWORK and CLIENT_CONNECT_STRING to the script bin/app-run-docker-test-data-generator so it can be called from different contexts.  First, rename the parameter CLIENT_CONNECT_STRING to GRPC_CONNECT_STRING (so we don't confuse it with mongo connect string).  Currently the values are hardwired in that script as variables.  I'd like to then add wrapper scripts that call that script with the appropriate parameters from the three docker-compose scenarios in docker/docker-compose/[dp-ecosystem, mongo-replica-set-single, and mongo-replica-set-multi].  The parameter values should be mapped as follows: 1) dp-ecosystem network = "dp-ecosystem_eco-network", connect string = "eco-envoy:8080"; 2) mongo-replica-set-single network = "mongo-replica-set-single_default", connect string = "dp-ingestion-server:50051"; 3) mongo-replica-set-multi network = "mongo-replica-set-multi_mongo-cluster", connect string = "dp-ingestion-server:50051".
